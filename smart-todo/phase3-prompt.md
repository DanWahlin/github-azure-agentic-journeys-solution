SmartTodo journey, Phase 3 of 3, deploy and verify. Read JOURNEY.md, PLAN.md, phase reports, all generated code, ../.github/skills/journey-runner/SKILL.md, and ../AGENTS.md. Work only in this smart-todo folder.

Use subscription [REDACTED], location westus, azd environment rr-smarttodo-0717, and resource group rg-rr-smarttodo-0717. Generate complete Bicep/azd infrastructure for Azure Functions Flex Consumption, Storage, Azure SQL, Microsoft Foundry/model deployment, monitoring, managed identities, and role assignments. Generate infra/hooks/postprovision.js as CommonJS that validates Go sqlcmd, uses argument arrays, creates the Function managed-identity SQL user/roles, applies schema and seed idempotently, switches Redirect to Proxy only when required, and restores firewall rules and original policy in finally. Never print tokens, passwords, or connection strings.

Run builds/tests and static Swift contract checks, validate Bicep/azure.yaml, run azd up, repair evidence-backed failures, and retry until successful. Verify the deployed Function is running and execute the complete live lifecycle: seed list for user-1, create, generate 3-7 AI steps, fetch, complete a step, delete, confirm absence. Always delete temporary verification data in finally. Update Config.swift with the deployed API URL. Write sanitized run-report.md and owned resource inventory. Create issues.md only for a genuinely new defect. After every live lifecycle check passes, delete only rg-rr-smarttodo-0717 and verify it no longer exists. Touch no unrelated resources.